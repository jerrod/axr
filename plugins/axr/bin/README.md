# bin/

Plugin-local validation script, invoked automatically by the marketplace `bin/validate`.

| File | Purpose |
|------|---------|
| `validate` | Rubric schema checks: total_points=100, weights sum to 100, every dimension has 5 criteria, every criterion has required fields |
