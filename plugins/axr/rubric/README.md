# rubric/

Versioned rubric JSON files — the source of truth for scoring.

| File | Status |
|------|--------|
| `rubric.v2.json` | **Current** — 9 dimensions, 45 criteria, 100 points |
| `rubric.v1.json` | Preserved for history comparison (8 dimensions, 40 criteria) |

Never edit a rubric in place — bump `rubric_version` instead. Trend data depends on version stability.
