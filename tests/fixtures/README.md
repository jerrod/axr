# Test Fixtures

Sample data for verifying checker behavior.

| Fixture | Purpose |
|---------|---------|
| `sample-rubric-output.json` | Expected output shape from a dimension checker |

These fixtures are consumed by `bin/test` for schema validation.
